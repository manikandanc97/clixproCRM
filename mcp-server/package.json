{
  "name": "clixprocrm-mcp-server",
  "version": "0.1.0",
  "description": "Model Context Protocol (MCP) Server Foundation for ClixProCRM",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "clixprocrm-mcp-server": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "test": "tsx --test src/**/*.spec.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^16.4.7",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
